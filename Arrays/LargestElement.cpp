#include<iostream>
#include<conio.h>
using namespace std;

int main()
{
    int n;
    int a[n];

    cout<<"Enter the size of array"<<endl;
    cin>>n;

    cout<<"Enter the element number "<<n<<endl;

    for(int i=0; i<n; i++)
    {
        cin>>a[n];
    }

    for(int i=0; i<n; i++)
    {
        cout<<"Elements in the array are : "<<a[n]<<" ";
    }
    getch;

    return 0;
}